import "./App.css";
import MedicineList from "./components/MedicineList";
const App = () => {
  // console.log(medicines.map((item) => item.name));
  return (
    <>
      <MedicineList />
    </>
  );
};

export default App;
